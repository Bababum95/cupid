import type { Variants } from "motion/react";

export const DELAY = 20000;

export const VARIANTS: Variants = {
  hidden: { opacity: 0, visibility: "hidden", display: "none" },
  visible: { opacity: 1, visibility: "visible", display: "flex" },
};

